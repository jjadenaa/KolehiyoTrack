{
  "buildCommand": "npm run build --workspace=@workspace/upcat && rm -rf dist && cp -r artifacts/upcat/dist/public dist",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
